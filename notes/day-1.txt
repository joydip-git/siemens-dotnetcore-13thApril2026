old concepts:
----------------
collections
delegates
reflection
new features in C#
LINQ
task and asynchronity

design pattern
------------------
Builder pattern


.NET Framework [legacy]
---------------------------
2002 (1.0) till 2019 (4.8)


2016
---------------------------------------------------------------------------
Open source, modular, cross-platform and light-weight framework - .Net Core
version: 1.0...3.1

.NET 
----------------
next version: 5, 6,...,10
LTS (even number) and STS (odd number)

CLI tool: dotnet
how many versions of .NET installed in your machine?
command: dotnet --list-sdks

.NET 10 => VS 2026

.NET Framework (legacy)
---------------------------------
base class library -> BCL (mscorlib.dll + system.dll + others)
CLR -> common language runtime
asp.net -> web tech
ado.net/ef -> data access

file structure and execubtables:
--------------------------------------
console app, wpf -> output file (.exe)

library, web app -> output file (.dll)

execution cycle of .NET
-------------------------

host app: shim.exe (windows)

mscoree.h (C++ header file) -> Microsoft Common Object Runtime Execution Engine (a file related to CLR)

mscoree.dll -> CLR

loadandBindClr ->

CSharp Compiler to compile C# code into IL code and generate assembly


.NET or .Net Core

execution cycle of .NET
-------------------------
host app???? -> application specific host is created

console app -> host file (.exe) + actual output file (.dll) or assembly + *.deps.json + *.runtimeconfig.json [platform neutral]

wpf -> host file (.exe) + actual output file (.dll) or assembly + *.deps.json + *.runtimeconfig.json [is not platform neutral]

web app -> host file/web server + actual output file (.dll) or assembly + *.deps.json + *.runtimeconfig.json [platform neutral] [platform neutral]

library -> actual output file (.dll) or assembly + *.deps.json [platform neutral]

Roslyn Compiler -> to compile C# code and VB code into IL code and generate assembly


base class library - core library
	small modular libraries
	
runtimes
	- core/.net runtime -> basic apps can run (console app)
		- desktop runtime -> wpf, windows form app
		- web runtime/asp.net core runtime -> web app
asp.net core -> web tech
ado.net/ef core -> data access


console -> namespace system -> mscorlib.dll
console -> namespace system -> system.console.dll


Microsoft.NETCore.App pack:
C:\Program Files\dotnet\packs\Microsoft.NETCore.App.Ref\10.0.5\ref\net10.0


Builder pattern

OCP => open for extension closed for change


Builder pattern:
---------------------------
	extend an existing entity with several features from outside the class using some other class
	using this builder pattern we build complex object one step at a time by following the same repeatative steps
	it helps you to avoid creating lots of subclasses

builders:
	The Builder pattern suggests that you extract the object construction code out of its own class and move it to separate objects called builders.

The pattern organizes object construction into a set of steps (buildWalls, buildDoor, etc.). To create an object, you execute a series of these steps on a builder object. 
The important part is that you don’t need to call all of the steps. You can call only those steps that are necessary for producing a particular configuration of an object.

extension method:
	adding instance methods in an existing without modifying the class source code


web app
	- add web server (kestrel)
	- add request and response pipe line
	

web app
	- add middleware for auth
	- add middleware for authorization
	- add middleware for cors

basic console app
	- add DI feature
	- add Logging feature
	- add feature to read config data from json file etc
	- add hosting feature


.Net Core or .NET 
----------------------
built-in features
1. Dependency Injection
2. Logging Service
3. Configuration service and options pattern
4. Hosting service







