Functional programming (FP):
----------------------------------------
It is a declarative way that treats computation as the evaluation of mathematical functions, avoiding mutable data and side effects. 
It focuses on immutability, pure functions, and function composition, resulting in cleaner, more predictable, and testable code compared to procedural or object-oriented approaches

Core Concepts
---------------
1. Pure Functions: Functions that return the same output for the same input and have no side effects (e.g., they don't change global state, modify arguments, or perform I/O).

2. Immutability: Data cannot be modified after it is created. Instead of changing data, new data structures are created.

3. First-Class Functions: Functions are treated as values, meaning they can be assigned to variables, passed as arguments, and returned from other functions. [delegates, lambda expressions are used for that]

4. Higher-Order Functions: Functions that take other functions as arguments or return them as results (e.g., map, filter, reduce). [again achieved through delegates]

5. Function Composition: The process of combining simple functions to build more complex ones.

6. Declarative Evaluation: Focusing on what to solve rather than how to solve it (imperative)

LINQ: Language Integrated Query
-------------------------------------------------
	It is a powerful feature in the .NET ecosystem that allows you to query and manipulate data using a consistent, declarative syntax directly within languages like C# and VB.NET
	It is the name for a set of technologies based on the integration of query capabilities directly into the C# language
	set of technologies: OOP, Generics, Collections, delegates, new geatures in C# (extension methods, lambda expressions, expression body, anonymous types, implcitly typed variable etc.) and functional programming

Traditionally, queries against data are expressed as simple strings without type checking at compile time or IntelliSense support. 

"select * from products"

Furthermore, you have to learn a different query language for each type of data source: SQL databases, XML documents, various Web services, and so on

With LINQ, a query is a first-class language construct, just like classes, methods, and events.

Write query expressions in a declarative query syntax. 
By using query syntax, you perform filtering, ordering, and grouping operations on data sources with a minimum of code. 
You use the same query expression patterns to query and transform data from any type of data source.

Query:
--------------
A query is an expression that retrieves data from a data source and this expression  can be written in 2 different styles
	a. method syntax
	b. query syntax

Three Parts of a Query Operation
--------------------------------------
All LINQ query operations consist of three distinct actions:

1. Obtain the data source.
2. Create the query.
3. Execute the query.

Immediate execution:
------------------------
It means that the data source is read and the operation is performed once. All the standard query operators that return a scalar result execute immediately. 
Examples of such queries are Count, Max, Average, and First 

or calls such as ToList<T>(), or iteration through the collection using foraech loop will force the query to be executed then and there.


Deferred execution:
--------------------
It means that the operation isn't performed at the point in the code where the query is declared. 
The operation is performed only when the query variable is enumerated, for example by using a foreach statement or ToList() method call.

install sql server express edition


data access:
--------------------
ADO.NET -> ActiveX Data Object .NET

built-in types to access data (APIs)

SQL Server (SQL Server Data Provider) [Microsoft.Data.SqlClient]
----------------------------------------
SqlConnection
SqlCommand
SqlDataReader
SqlDataAadapter
....

Oracle (Oracle Data Provider) [Oracle.Data.SqlClient]
---------------------------------------------------------
OracleConnection
OracleCommand
OracleDataReader
OracleDataAadapter
....


access data from SQL Server:
-------------------------------
System.data.sqlclient -> deprecated
Microsoft.Data.SqlClient

				keyword
name of server: 	"server" or "data source"
name of database: 	"database" or "initial catalog"

authentication:
windows			"integrated security=true; trust server certificate=true"

sql server		"user id=<value>;password=<value>;trust server 		                                     							certificate=true"
			"uid=<value>;pwd=<value>;trust server certificate=true"


connection string: (with windows authentication)
"server=joydip-pc\\sqlexpress;database=siemensdb;integrated security=true; trust server certificate=true"
"data source=joydip-pc\\sqlexpress;initial catalog=siemensdb;integrated security=true; trust server certificate=true"

connection string: (with sql server authentication)
"server=joydip-pc\\sqlexpress;database=siemensdb;user id=<value>;password=<value>; trust server certificate=true"

"data source=joydip-pc\\sqlexpress;initial catalog=siemensdb;uid =<value>; pwd=<value>; trust server certificate=true"


Data Source=JOYDIP-PC\\SQLEXPRESS;Integrated Security=True;Connect Timeout=30;Encrypt=True;Trust Server Certificate=True;Application Intent=ReadWrite;Multi Subnet Failover=False;Command Timeout=30


SqlCommand method to execute query
------------------------------------------
select query:
--------------------
1. ExecuteReader -> use when executing a select query which returns more than one value. This method creates a SqlReader object (buffer) and stores the retrieved data in that reader

2. ExecuteScalar -> use when executing a select query which returns one value.

other DDL (insert, update, delete, create table, create database, alter table etc.)
-------------------------------------------
3. ExecuteNonQuery() -> use it for all other non-select query. it returns an intereger value indicating how many records, tables etc. got affected


against XML file:
-----------------------
4. ExecuteXmlReader() -> used with XML file and returns a reader object with data from XML file

Disadvantages:
-------------------
1. the change of database will force the developers to change the data access code and the provider

2. developers need to know database specific SQL or if it is no-SQL database then specific drivers and query techniques

3. developers need to micromanage the connection, the command and many such data provider APIs, such as opening and closing connection, disposing off unmanaged resources etc.

Entity Framework -> OR/M tool
EF Core


.NET Features:
---------------------
Configuration services: "reading" configuration data from different sources
	- text file
	- json file
	- xml file
	- env variable file
	- azure vault
	- runtime collection
	....
through a common interface -> IConfiguration (-> IConfigurationRoot)

packages:

   Core: provides only reading from in-memory collection
	Microsoft.Extensions.Configuration (implementation)
	Microsoft.Extensions.Configuration.Abstractions (abstraction)

   Json:
	Microsoft.Extensions.Configuration.Json

   Binder:
	Microsoft.Extensions.Configuration.Binder

Note: Configuration services are already registered with .NET DI container

Binding a model/strongly-typed class to a section of json file

Microsoft.Extensions.Configuration.Binder:
---------------------------------------------
Provides the functionality to bind an object to data in configuration providers for Microsoft.Extensions.Configuration. 
This package enables you to represent the configuration data as strongly-typed classes defined in the application code. 
To bind a configuration, use the "Microsoft.Extensions.Configuration.ConfigurationBinder.Get" extension method on the IConfiguration object. 
To use this package, you also need to install a package for the configuration provider, for example, Microsoft.Extensions.Configuration.Json for the JSON provider.


adding nuget packages
	- NuGet Package Manager tool
	- NuGet Package Manager Console
	  ex: NuGet\Install-Package Microsoft.Extensions.DependencyInjection -Version 10.0.6
	- dotnet cli
	  ex: dotnet add package Microsoft.Extensions.DependencyInjection --version 10.0.6
	- Package Reference
	  copy the package reference and paste the same in *.csproj file in <ItemGroup> element
	







